# ===============================
# Example Environment Variables
# ===============================
#
# DO NOT COMMIT YOUR .env FILE TO VERSION CONTROL.
# This file is an example. Copy it to a new file named .env and fill in the values.
#

# --- Application Environment ---
# Set to 'development' for local work, 'production' for deployment.
NODE_ENV=development

# --- Server Port ---
# The port the application will run on. Render may set this automatically.
PORT=3001

# --- Frontend URL ---
# The URL of your frontend application for CORS policy.
# In production, this is the live URL of your site (e.g., https://www.your-app.com).
# The default for development assumes the frontend is served from the same origin.
FRONTEND_URL=http://localhost:3001

# --- Database Configuration ---
# Your PostgreSQL database connection string.
# Get this from your database provider (e.g., Render, AWS RDS).
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# --- Session Secret ---
# A long, random, and secret string used to sign session ID cookies.
# Generate a new one for your project. You can use an online generator.
SESSION_SECRET=your-super-strong-and-secret-key-here